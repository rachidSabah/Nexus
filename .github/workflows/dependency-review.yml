name: dependency-review
# Disabled: Requires Dependency Graph to be enabled in repository settings
# To enable: Go to https://github.com/rachidSabah/Nexus/settings/security_analysis
# and enable "Dependency graph"
on: [pull_request]

permissions:
  contents: read
  pull-requests: write

jobs:
  dependency-review:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # Temporarily disabled due to missing Dependency Graph configuration
      # Uncomment below after enabling Dependency Graph in repo settings
      # - uses: actions/dependency-review-action@v4
