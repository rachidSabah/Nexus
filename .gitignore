# Dependencies
node_modules/
.pnp
.pnp.js

# pnpm content-addressable store & turbo cache (local, never commit)
.pnpm-store/
.turbo/

# Build artifacts
dist/
build/
.next/
.turbo/
*.tsbuildinfo
coverage/

# Environment / secrets — NEVER commit real credentials
.env
.env.*
!.env.example
.env.local
.env.*.local
.envrc
*.local
vault.json
*.pem
*.key
*.db
*.sqlite

# IDE
.idea/
.vscode/
*.swp
*.swo
.DS_Store

# Logs (may contain local machine paths)
*.log
*.e2e.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Gateway runtime logs
apps/gateway/*.log
apps/gateway/*.err.log

# Local helper artifacts / machine-specific scripts
*.lnk
convert_icon.ps1
create_shortcut.ps1

# OS / editor
Thumbs.db
ehthumbs.db

# Test / coverage output
.nyc_output/
*.lcov
.cache/

# Local agent state & workspaces (keys, memory, rag, workspaces)
.agent-nexus/
.nexus/
applications/
.tmp/
tmp/
*.secret
*.p12
*.crt

# Isolated test vault / scratch artifacts (NEVER commit; uses a COPY of the prod vault)
.test-island/
.tmp_*.json

# Isolated debug worktrees (agentic debug playground — never commit)
.worktrees/
