# NEXUS PHASE 2 — UNIVERSAL CODING-AGENT RUNTIME GAP MATRIX

| COMPONENT | CURRENT STATE | GAP | IMPACT | IMPLEMENTATION PLAN | TEST |
|-----------|---------------|-----|--------|---------------------|------|
| **Local Agent Auto-Configuration** | `AgentDetector` in `apps/gateway/src/agent-detector.ts` detects 18 agent binaries/packages across PATH and home config dirs. | Endpoint `/v1/agents/connect` and CLI `nexus agent connect <id>` were not fully wired in server HTTP routes for one-click configuration. | User had to manually invoke integration CLI commands or manually edit config files. | Add `POST /v1/agents/connect` to `server.ts` leveraging `@anx/integrations` `BUILTIN_INTEGRATIONS`. | `agent-connector.test.ts` |
| **Agent Connector Architecture** | 18 adapters implemented in `@anx/integrations` (`ClaudeCodeIntegration`, `CodexCliIntegration`, `OpenCodeIntegration`, `AiderIntegration`, `ClineIntegration`, etc.). | Expose unified `AgentConnectorManager` in `@anx/integrations` to handle status check, configuration backup, installation, uninstallation, and verification cleanly. | Seamless multi-agent management across CLI and HTTP API. | Create `agent-connector-manager.ts` in `packages/integrations/src/`. | `agent-connector.test.ts` |
| **Claude Code Special Handling** | `projectClaudeCatalog` exposes `claude-gw-*` aliases; `requiresRestart: true` is reported. | Expose dynamic refresh endpoint `POST /v1/agents/refresh/claude-code` and version mismatch warnings. | Informs user safely when Claude Code CLI process restart is needed. | Add `refresh` route handling and process status check in `server.ts`. | `agent-connector.test.ts` |
| **Universal /model Catalog API** | `/v1/models` and `/v1/debug/models/agents` expose catalog items and agent matrices. | Expose unified `/v1/catalog` endpoint returning `catalogVersion`, `generatedAt`, `providers`, `models`, `agents`, and `policies`. | Single structured endpoint for dashboard and CLI tools to consume full ecosystem metadata. | Add `GET /v1/catalog` endpoint in `server.ts`. | `runtime.test.ts` |
| **Nexus Doctor** | Individual health/stats routes (`/health`, `/v1/models/stats`) exist. | Unified `/v1/doctor` endpoint and CLI `nexus doctor` command summarizing readiness, registry health, active keys, agents, and token efficiency. | One-command diagnostic check for local developer setup. | Add `GET /v1/doctor` endpoint in `server.ts`. | `runtime.test.ts` |
| **Context Fingerprinting & Cache** | `PromptCompressor` and `ContextWindowManager` track token metrics. | `ContextCache` in `@anx/token-efficiency` to hash `contextHash`, `toolSchemaHash`, and `systemPromptHash` to measure bytes/tokens avoided. | Eliminates unnecessary internal payload re-processing for identical turns. | Create `context-cache.ts` in `packages/token-efficiency/src/`. | `token-counter.test.ts` |
