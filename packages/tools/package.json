{
  "name": "@anx/tools",
  "version": "0.5.0",
  "description": "Tool Management — permissions, sandboxing, discovery, logging for agent tools",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist coverage .turbo"
  },
  "dependencies": {
    "@anx/core": "workspace:*",
    "@anx/shared": "workspace:*"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.8"
  }
}
