{
  "name": "@anx/token-efficiency",
  "version": "0.1.0",
  "description": "Token-efficiency layer for coding agents: SAFE-mode deduplication, token estimation, and savings metrics",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsup",
    "test": "vitest run",
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist coverage .turbo"
  }
}