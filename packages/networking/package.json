{
  "name": "@anx/networking",
  "version": "0.5.0",
  "description": "Networking: HTTP/HTTPS/SOCKS5 proxy, DNS-over-HTTPS, failover, diagnostics",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsup",
    "test": "vitest run",
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist coverage .turbo"
  },
  "dependencies": {
    "@anx/core": "workspace:*"
  },
  "optionalDependencies": {
    "undici": "^8.10.1"
  }
}
